#![forbid(unsafe_code)]

//! Builds the xv6 file-system image (`fs.img`) from the on-disk types in
//! `abi`. The implementation lands with M5; for now the binary exercises
//! the host build pipeline and the `abi` dependency.

fn main() {}
